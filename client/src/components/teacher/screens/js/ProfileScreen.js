import { React, useState } from 'react'
import axios from 'axios'
import ResponsiveDrawer from '../../../common/TeacherDrawer';

const ProfileScreent = () => {
    return (
        <div>
            <ResponsiveDrawer name="Profile" />

        </div>
    )
}

export default ProfileScreent;
